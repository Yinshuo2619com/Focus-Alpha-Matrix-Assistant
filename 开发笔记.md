# 开发笔记

## 头像上传功能问题排查与修复

### 问题 1：上传接口 404
- **现象**：前端日志显示 `status: 404`，URL 为 `/api/user/avatar`
- **原因**：`el-upload` 的 `action="/api/user/avatar"` 是相对路径，请求发到了 Vite dev server (5173) 而非后端 (8080)
- **修复**：在 `frontend/vite.config.ts` 中添加代理配置，将 `/api` 和 `/avatar` 转发到 `http://localhost:8080`

### 问题 2：java.sql.Date 无法强转为 LocalDate
- **现象**：`class java.sql.Date cannot be cast to class java.time.LocalDate`
- **原因**：`UserServiceImpl.uploadAvatar` 中从数据库读取 `last_upload_date` 时直接强转为 `LocalDate`，但 JDBC 返回的是 `java.sql.Date`
- **修复**：先转为 `java.sql.Date`，再调用 `.toLocalDate()` 转换（`UserServiceImpl.java:120-122`）

### 问题 3：登录后头像始终为 null
- **现象**：上传成功但刷新后头像不显示，数据库中 avatar 字段有值
- **原因**：`UserServiceImpl.login` 方法中 ResultSet 手动映射字段时**漏掉了 `avatar` 字段**
- **修复**：在 login 的 ResultSet 映射中添加 `u.setAvatar(rs.getString("avatar"))`（`UserServiceImpl.java:49`）

### 问题 4：上传成功后刷新页面头像丢失
- **现象**：上传后头像显示正常，但刷新页面后恢复默认头像
- **原因**：`Profile.vue` 的 `handleUploadSuccess` 只更新了内存中的 store，没有同步写入 `localStorage`
- **修复**：上传成功后增加 `localStorage.setItem('userInfo', JSON.stringify(userStore.userInfo))`

### 问题 5：头像显示为灰色，看不到实际图片
- **现象**：`el-avatar` 组件始终显示灰色背景，不渲染实际图片
- **原因**：Element Plus 的 `el-avatar` 组件在某些场景下自带灰色背景层会遮挡图片
- **修复**：将 `el-avatar` 替换为原生 `<img>` 标签，配合 CSS `border-radius: 50%` + `object-fit: cover` 保持圆形效果

### 问题 6：头像 URL 返回 403 Forbidden
- **现象**：`curl http://localhost:8080/avatar/admin3.jpg` 返回 403
- **原因**：Spring Security 拦截了 `/avatar/**` 的静态资源请求，被 `anyRequest().authenticated()` 拦截
- **修复**：在 `SecurityConfig.java` 中添加 `.requestMatchers("/avatar/**").permitAll()` 放行头像资源

### 问题 7：头像文件存储路径不一致
- **现象**：文件写入和静态资源映射指向不同目录
- **原因**：`UserServiceImpl` 写文件用相对路径 `uploads/avatar`（相对于工作目录），`WebConfig` 的 `file:uploads/avatar/` 也相对工作目录，但 Spring Boot 从不同目录启动时路径不一致；旧代码用 `src/main/resources/avatar`（classpath 下不可写）
- **修复**：引入 `application.yml` 配置项 `avatar.upload-dir: uploads/avatar`，`UserServiceImpl`、`WebConfig`、`AdminController` 统一通过 `@Value` 注入同一配置

### 问题 8：管理员上传头像也用了旧路径
- **现象**：`AdminController` 的头像上传保存到 `src/main/resources/avatar`（旧路径）
- **原因**：路径硬编码，没有和 `UserServiceImpl` 同步修改
- **修复**：`AdminController` 也改为通过 `@Value("${avatar.upload-dir}")` 注入配置项

### 问题 9：用户管理页面头像不显示
- **现象**：管理员后台用户列表中所有头像都显示默认头像
- **原因**：`AdminController.getUsers` 的 SQL 查询 `SELECT id, username, nickname, email, phone, status, created_at FROM user` **没有包含 `avatar` 列**，前端 `row.avatar` 始终为 `undefined`
- **修复**：SQL 中添加 `avatar` 列（`AdminController.java:78`）

### 问题 10：头像路径缺少后端地址前缀
- **现象**：数据库存的是 `/avatar/admin3.jpg`，浏览器请求 `localhost:5173/avatar/admin3.jpg`（Vite dev server），而非后端
- **原因**：头像路径是相对路径，前端直接当 `<img src>` 使用
- **修复**：在 `user.ts` store 的 `userAvatar` getter 中拼接 `http://localhost:8080` 前缀，管理员页面的 `getAvatarUrl` 函数同理

### 相关配置（application.yml）
```yaml
avatar:
  upload-dir: uploads/avatar
```

### 关键文件清单
| 文件 | 作用 |
|------|------|
| `frontend/vite.config.ts` | Vite 代理配置 |
| `frontend/src/stores/user.ts` | 用户 store，头像 URL 拼接 |
| `frontend/src/views/Profile.vue` | 个人中心页面 |
| `frontend/src/views/admin/UserManagement.vue` | 管理员用户管理页面 |
| `backend/.../UserController.java` | 用户头像上传接口 |
| `backend/.../AdminController.java` | 管理员接口（用户列表+头像上传） |
| `backend/.../UserServiceImpl.java` | 用户服务实现 |
| `backend/.../SecurityConfig.java` | Spring Security 配置 |
| `backend/.../WebConfig.java` | 静态资源映射 |
| `backend/.../FileValidator.java` | 文件类型校验（魔数） |
| `backend/.../application.yml` | 应用配置 |

---

## 课表刷新功能开发笔记

### 功能概述

用户点击"刷新课表"按钮 → 后端调用教务系统 JSON API 获取最新课表数据 → 解析并保存到数据库 → 前端展示。

### 关键文件

| 文件 | 作用 |
|------|------|
| `backend/.../controller/ScheduleController.java` | 刷新端点 + JSON 解析逻辑 |
| `backend/.../service/EduProxyService.java` | 教务系统代理（session 管理、API 调用） |
| `backend/.../service/impl/ScheduleServiceImpl.java` | 课表持久化 |
| `backend/.../dto/SaveScheduleRequest.java` | 保存请求 DTO（含 currentWeek 字段） |
| `frontend/src/components/ScheduleGrid.vue` | 课表网格展示 + 周次自动选择 |
| `frontend/src/views/Home.vue` | 首页，传递 currentWeek 给 ScheduleGrid |
| `frontend/src/types/schedule.ts` | TypeScript 类型定义 |

### 问题 1：教务系统 JSON API 格式

教务系统（Wisedu/超星）返回的课表数据结构：
- 课程名在 `lessons[].course.nameZh`（不是 `lessons[].nameZh`，后者是班级名）
- 时间地点信息在 `lessons[].scheduleText.dateTimePlacePersonText`
- 该字段是中文格式，多条记录用 `;` 分隔
- 格式示例：`1~16周 周一 第一节~第二节 龙山校区 A507 邹璐`

### 问题 2：Data too long for column 'teacher'

- **现象**：刷新课表时报数据库字段长度溢出错误
- **原因**：`dateTimePlacePersonText` 包含多条用 `;` 分隔的记录，正则只匹配了第一条，导致 teacher 字段包含了后续所有内容（数百字符）
- **修复**：先按 `;` 拆分，再逐条用正则解析

### 问题 3：课程名称错误

- **现象**：课表上显示的是班级名而非课程名
- **原因**：使用了 `lesson.nameZh`（班级名，如"计算机科学与技术(工)2023级2班"），而非 `lesson.course.nameZh`（课程名，如"劳动教育（6）"）
- **修复**：优先从 `lesson.course.nameZh` 提取课程名

### 问题 4：当前周次显示错误（显示第11周，实际第9周）

- **现象**：前端课表显示的当前周次与教务系统不一致
- **原因**：前端 `ScheduleGrid.vue` 用日期推算周次，第二学期起始日期硬编码为 2月17日，但实际开学约在 3月3日，偏差约2周
- **修复**：
  - 后端：从教务系统 API 响应中提取 `currentWeek` 字段，存入数据库 `schedule.current_week` 列
  - 前端：优先使用教务系统返回的 `currentWeek`，日期推算仅作兜底

### 问题 5：调试代码清理

开发过程中遗留的调试代码：
- `[Refresh] Raw JSON` 日志（打印 5000 字符 JSON）
- `[Parse]` 每条课程解析日志
- `/debug/html` 调试端点
- `EduProxyService` 中泄露加密密码的日志
- `EduProxyController` 中注释掉的代码

全部清理完毕。

### 技术决策

1. **currentWeek 存储**：教务系统返回的 currentWeek 比前端日期推算更准确，存入数据库避免依赖前端计算
2. **日期推算兜底**：当用户长时间不刷新时，前端仍能基于学期起始日期推算周次（第一学期 9月1日，第二学期 2月17日）
3. **中文数字解析**：教务系统用中文数字（第一节~第二节），实现了 `chineseToInt()` 方法转换
4. **周次字符串格式**：支持 `1-16`、`1~16`、`1,3,5`、`1~4,7~17` 等多种格式

### 数据库变更

```sql
ALTER TABLE schedule ADD COLUMN current_week INT DEFAULT NULL COMMENT '教务系统返回的当前周次';
```

---

## 电费功能紧急修复（2026-06-15）

### Bug 1：昨日耗电显示错误

- **现象**：首页电费卡片"昨日耗电"显示 353.89 度，明显异常
- **原因**：`getRealtimeBalance()` 查询 `SELECT consumption ... WHERE consumption IS NOT NULL ORDER BY record_date DESC LIMIT 1`，返回的是**最近一条 consumption 不为 NULL 的记录**，而非昨天的值。采集时当天 consumption 先存 NULL（等 `recalcYesterday()` 回算），导致查到很旧的错误数据
- **修复**：改为从昨天开始往前找最多 7 天，找到第一条有效的 consumption。`getSummary()` 同样修复

### Bug 2：排行榜时间逻辑错误

- **现象**：凌晨 4 点前排行榜显示为空（当天数据还没采集）
- **原因**：排行日期固定为 `LocalDate.now().minusDays(1)`，没有考虑凌晨 4 点采集时间
- **修复**：4 点前 → `today.minusDays(2)`（显示前天排行榜）；4 点后 → `today.minusDays(1)`（显示昨天排行榜）

### Bug 3：recalcConsumption() 未级联更新次日

- **现象**：用户修改充值记录后，当天 consumption 正确但次日仍为 NULL
- **原因**：`saveRecharge()` 只调用 `recalcConsumption(roomId, date)` 重算当天，但当天 consumption 变化会影响次日计算（次日的 `prev_balance + recharge - today_balance`）
- **修复**：`recalcConsumption()` 改为级联更新当天和次日，抽取 `calcOneDay()` 方法

### Bug 4：INSERT IGNORE 导致数据无法修复

- **现象**：168 个房间 6/14 的 consumption 为 NULL，`recalcYesterday()` 对大部分房间未生效
- **原因**：`collectAll()` 用 `INSERT IGNORE`，同一天第二次采集时跳过已有记录，`recalcYesterday()` 没机会重跑。首次采集时部分房间 `recalcYesterday()` 静默失败（异常被 catch 吞掉），后续无法修复
- **修复**：`INSERT IGNORE` 改为 `INSERT ... ON DUPLICATE KEY UPDATE balance = VALUES(balance)`，重复采集时更新 balance 并重新触发 `recalcYesterday()`；`recalcYesterday()` 添加 warn 级别日志

### Bug 5：Redis 缓存空排行榜

- **现象**：数据库修正后排行榜仍为空
- **原因**：`computeAndCacheRanking()` 缓存了空结果，TTL 25 小时
- **修复**：手动清缓存 `redis-cli DEL elec:ranking:v2:96:2026-06-14:top elec:ranking:v2:96:2026-06-14:bottom`

### 充值记录结构变更

- **变更**：`electricity_recharge` 表新增 `price` 列（电价，元/度），`amount` 改为由 `kwh × price` 自动计算
- **DDL**：`ALTER TABLE electricity_recharge ADD COLUMN price DECIMAL(10,2) AFTER kwh;`
- **前端**：充值弹窗改为填 kwh + price，自动显示计算金额
- **后端**：`saveRecharge()` 接收 kwh + price，计算 amount 存入

### 关键文件

| 文件 | 作用 |
|------|------|
| `backend/.../service/ElectricityService.java` | 核心修复：recalcYesterday、recalcConsumption、getRealtimeBalance、getRanking、saveRecharge |
| `backend/.../controller/ElectricityController.java` | saveRecharge 接口参数变更 |
| `frontend/src/views/ElectricityDetail.vue` | 充值弹窗、提交逻辑、记录显示 |
| `frontend/src/components/ElectricityCard.vue` | 首页电费卡片（调用 /realtime 接口） |
